#include <iostream>
#include <cmath>
#include <algorithm>
#include <queue>
#include <bitset>
#include <set>
#include <vector>
#include <cstring>
using namespace std;

char s[101];
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);	
	cin >> s;
    cout << strlen(s);
    
	return 0; 
}