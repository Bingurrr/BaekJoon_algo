#include <iostream>
#include <string>


using namespace std;
string s;
int ans;
int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
    while(cin >> s){
        ans++;
    }
    cout << ans;
}